# Tarot Luna - Telegram Mini App

Telegram Mini App для Таро-раскладов с интеграцией AI (OhMyGPT) и оплатой YooMoney.

## 🌙 Структура проекта

```
├── src/                    # React фронтенд (Vite + Tailwind)
├── public/assets/cards/    # 78 карт Rider-Waite + рубашка
├── ТАРО/                   # Python бэкенд (aiogram + aiohttp)
│   ├── main.py            # Основной файл бота
│   ├── api.py             # API эндпоинты для Mini App
│   ├── database.py        # SQLite база данных
│   ├── handlers.py        # Обработчики команд бота
│   ├── ohmygpt_api.py     # Интеграция с AI
│   ├── yoomoney.py        # Платежи YooMoney
│   └── config.py          # Конфигурация
└── Карты/                  # Исходные изображения карт
```

## 🚀 Запуск

### Фронтенд (локально)

```bash
npm install
npm run dev
```

### Бэкенд (локально)

```bash
cd ТАРО
pip install -r requirements.txt
cp .env.example .env
# Отредактируйте .env с вашими ключами
python main.py
```

## 📱 Деплой

### Фронтенд на Vercel

1. Push в GitHub
2. Подключите репозиторий к Vercel
3. Build command: `npm run build`
4. Output directory: `dist`

### Бэкенд на VPS

```bash
# На сервере
git clone your-repo
cd your-repo/ТАРО
pip install -r requirements.txt
# Настройте .env
python main.py
# Или используйте systemd/supervisor для автозапуска
```

### Настройка BotFather

1. `/setmenubutton` в @BotFather
2. Выберите вашего бота
3. Введите URL: `https://your-vercel-url.vercel.app`
4. Введите текст кнопки: `🔮 Открыть Таро`

## 🔌 API Эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| POST | /api/auth | Верификация initData |
| GET | /api/user | Данные пользователя |
| POST | /api/reading | Создать расклад |
| GET | /api/history | История раскладов |
| POST | /api/payment | Ссылка на оплату |
| GET | /api/achievements | Достижения |
| GET | /api/rates | Тарифы |

## ✨ Фичи

- 🃏 78 карт Rider-Waite с анимацией переворота
- 🤖 AI-интерпретации через OhMyGPT
- 💳 Оплата через YooMoney (polling)
- 🏆 Система достижений и уровней
- 🤝 Реферальная программа
- 📊 Статистика и история
